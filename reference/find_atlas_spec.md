# Look Up an Atlas Spec by ID or Alias

Look Up an Atlas Spec by ID or Alias

## Usage

``` r
find_atlas_spec(name)
```

## Arguments

- name:

  Character scalar; an id or alias, matched
  case/underscore-insensitively.

## Value

The matching \`atlas_spec\` list.
