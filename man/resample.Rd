% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schaefer.R
\name{resample}
\alias{resample}
\title{Resample Volume to New Space}
\usage{
resample(
  vol,
  outspace,
  smooth = FALSE,
  interp = 0,
  radius = NULL,
  min_neighbors = 3
)
}
\arguments{
\item{vol}{A NeuroVol object to be resampled}

\item{outspace}{A NeuroSpace object specifying the target space}

\item{smooth}{Logical. Whether to apply boundary smoothing after resampling.
Default: FALSE}

\item{interp}{Integer. Interpolation method (0=nearest neighbor, 1=linear).
Default: 0}

\item{radius}{Numeric. Radius for smoothing neighborhood in voxels.
If NULL, uses max(spacing)+0.5. Default: NULL}

\item{min_neighbors}{Integer. Minimum number of neighbors required for smoothing.
Default: 3}
}
\value{
A resampled NeuroVol object in the new space
}
\description{
Resamples a volume to a new space with optional smoothing of parcel boundaries.
This is particularly useful for atlas parcellations where maintaining discrete
labels is important.
}
\details{
The resampling process:
\itemize{
  \item First performs nearest-neighbor interpolation to the new space
  \item Optionally smooths boundaries using a local majority voting scheme
  \item Preserves zeros in the mask (background)
}
}
