% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coordinate_spaces.R
\name{transform_vertices_to_volume}
\alias{transform_vertices_to_volume}
\title{Transform Surface Vertices to Volume Space}
\usage{
transform_vertices_to_volume(
  vertices,
  surface_template,
  target_space = "MNI152"
)
}
\arguments{
\item{vertices}{Numeric matrix of vertex coordinates (N x 3).}

\item{surface_template}{Character string identifying the surface template
the vertices come from (e.g., "fsaverage", "fsLR").}

\item{target_space}{Character string identifying the target coordinate
space (e.g., "MNI152"). Default is "MNI152".}
}
\value{
Transformed vertex coordinates (N x 3 matrix). Returns the input
  unchanged if no transform is needed.
}
\description{
Convenience function to transform surface vertex coordinates to match
a target volumetric coordinate space.
}
\details{
This is a convenience wrapper around \code{\link{transform_coords}} that
automatically determines the source space from the surface template.
}
\examples{
\dontrun{
# Load fsaverage surface
surf <- load_surface_template("fsaverage", "white", hemi = "L")
verts <- neurosurf::vertices(surf)

# Transform vertices to MNI152 for sampling from fMRI volume
verts_mni152 <- transform_vertices_to_volume(verts, "fsaverage", "MNI152")
}

}
\seealso{
\code{\link{transform_coords}}, \code{\link{get_surface_coordinate_space}}
}
