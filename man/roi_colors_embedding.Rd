% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roi_colors.R
\name{roi_colors_embedding}
\alias{roi_colors_embedding}
\title{Embedding-based palette with smooth gradients}
\usage{
roi_colors_embedding(
  rois,
  id_col = "roi",
  feature_cols = c("x", "y", "z"),
  hemi_col = NULL,
  method = c("umap", "pca"),
  C_range = c(40, 90),
  L = 65,
  seed = 1
)
}
\arguments{
\item{rois}{Tibble with one ROI per row containing `roi`, `x`, `y`, and `z`.}

\item{id_col}{Column containing ROI IDs.}

\item{feature_cols}{Character vector of numeric or categorical columns that
will be embedded (passed to `stats::model.matrix()`).}

\item{hemi_col}{Optional hemisphere column used for the final light/dark
adjustment.}

\item{method}{Either `"umap"` (requires the `uwot` package) or `"pca"`.}

\item{C_range}{Chroma range mapped from radial distance in the embedding.}

\item{L}{Lightness value used for all ROIs before hemispheric adjustments.}

\item{seed}{Random seed for reproducibility.}
}
\value{
Tibble with ROI IDs and colours.
}
\description{
Project ROI features to two dimensions (UMAP or PCA) and map polar angle to
hue, yielding palettes that look globally structured.
}
\examples{
\donttest{
rois <- data.frame(
  roi = 1:10, network = rep(c("Vis", "DMN"), 5),
  x = runif(10), y = runif(10), z = runif(10)
)
pal <- roi_colors_embedding(rois, feature_cols = c("x","y","z"),
                             method = "pca")
}

}
