% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atlas_download.R
\name{.neuroatlas_try_download}
\alias{.neuroatlas_try_download}
\title{Download an Atlas Asset (soft).}
\usage{
.neuroatlas_try_download(
  url,
  dest = NULL,
  mode = "wb",
  quiet = TRUE,
  min_size = 1024L,
  description = "atlas asset"
)
}
\value{
A list with fields:
  * `ok`: `TRUE` on success.
  * `path`: path to the downloaded file (or `NULL`).
  * `error`: the captured error condition on failure (or `NULL`).
  * `lfs_pointer`: `TRUE` if the file looked like a Git LFS/git-annex
    pointer stub.
}
\description{
Wraps [.neuroatlas_download()] but returns a status list instead of
throwing, so callers can implement fallback strategies while still having
access to the full error condition (rather than a silent `NULL`).
}
\keyword{internal}
