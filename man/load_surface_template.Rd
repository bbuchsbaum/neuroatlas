% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/template_flow.R
\name{load_surface_template}
\alias{load_surface_template}
\title{Load a surface template as a neurosurf geometry}
\usage{
load_surface_template(
  template_id,
  surface_type,
  hemi = c("L", "R", "both"),
  density = NULL,
  resolution = NULL,
  ...
)
}
\arguments{
\item{template_id}{Surface template identifier passed to TemplateFlow
(e.g., "fsaverage", "fsaverage6", "fsLR").}

\item{surface_type}{Surface type (e.g., "white", "pial", "inflated",
"midthickness").}

\item{hemi}{Hemisphere to load. One of "L", "R", or "both". If "both",
a named list with elements \code{L} and \code{R} is returned.}

\item{density}{Optional surface density (TemplateFlow \code{density}
argument).}

\item{resolution}{Optional resolution string (TemplateFlow \code{res}
argument), e.g., "06" for fsaverage6.}

\item{...}{Additional arguments forwarded to \code{\link{get_surface_template}},
such as alternative cache directories.}
}
\value{
A \code{neurosurf::SurfaceGeometry} object when \code{hemi} is "L"
  or "R"; a named list of two \code{SurfaceGeometry} objects when
  \code{hemi} is "both".
}
\description{
Convenience wrapper around \code{\link{get_surface_template}} that
downloads (via TemplateFlow) the requested surface geometry and returns it
as a \code{neurosurf::SurfaceGeometry} object (or a left/right list).
}
\examples{
\dontrun{
  # fsaverage6 pial surface as NeuroSurface
  lh <- load_surface_template("fsaverage", "pial", hemi = "L",
                              density = "41k", resolution = "06")

  # Both hemispheres of fsLR 32k inflated surface
  both <- load_surface_template("fsLR", "inflated", hemi = "both",
                                density = "32k")
}
}
