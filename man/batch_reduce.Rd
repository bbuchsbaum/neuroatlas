% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch_reduce.R
\name{batch_reduce}
\alias{batch_reduce}
\title{Batch Reduce Multiple Volumes by an Atlas}
\usage{
batch_reduce(
  inputs,
  atlas,
  stat_func = mean,
  ...,
  format = "long",
  parallel = FALSE,
  .progress = TRUE
)
}
\arguments{
\item{inputs}{A named list of inputs. Each element can be:
\itemize{
  \item A \code{NeuroVol} (3D) or \code{NeuroVec} (4D) object
  \item A character string file path (read via \code{neuroim2::read_vol})
}
If unnamed, subjects are auto-named \code{"sub_001"}, \code{"sub_002"}, etc.}

\item{atlas}{An atlas object.}

\item{stat_func}{Function to apply within each ROI (default: \code{mean}).}

\item{...}{Additional arguments passed to \code{\link{reduce_atlas}}.}

\item{format}{Character, output format passed to \code{reduce_atlas}.
Default \code{"long"}.}

\item{parallel}{Logical. If \code{TRUE}, uses
\code{future.apply::future_lapply()} for parallel processing.
Requires the \pkg{future.apply} package.}

\item{.progress}{Logical. If \code{TRUE} (default), displays a
\code{cli} progress bar.}
}
\value{
A tibble with a \code{subject} column prepended to the
  \code{reduce_atlas} output for each input.
}
\description{
Applies \code{\link{reduce_atlas}} to multiple input volumes (or file
paths) and combines the results into a single tibble with a
\code{subject} column identifying each input.
}
\examples{
\dontrun{
# With NeuroVol objects
vols <- list(sub01 = vol1, sub02 = vol2, sub03 = vol3)
results <- batch_reduce(vols, atlas, mean)

# With file paths
files <- list(sub01 = "path/to/sub01.nii.gz",
              sub02 = "path/to/sub02.nii.gz")
results <- batch_reduce(files, atlas, mean, parallel = TRUE)
}

}
\seealso{
\code{\link{reduce_atlas}} for single-volume extraction
}
