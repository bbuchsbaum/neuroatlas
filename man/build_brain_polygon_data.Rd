% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ce_shared_exports.R
\name{build_brain_polygon_data}
\alias{build_brain_polygon_data}
\title{Build brain polygon render data for a surface atlas}
\usage{
build_brain_polygon_data(surfatlas, views, surface, projection_smooth = 0L)
}
\arguments{
\item{surfatlas}{A surfatlas object (e.g. from \code{\link{schaefer_surf}()}).}

\item{views}{Character vector of views to render
(e.g. \code{c("lateral", "medial")}).}

\item{surface}{Character: surface type (\code{"inflated"}, \code{"pial"},
\code{"white"}).}

\item{projection_smooth}{Integer: number of Laplacian smoothing iterations
on projected coordinates (default \code{0L}).}
}
\value{
A list with two elements:
  \describe{
    \item{\code{polygons}}{Tibble of 2D projected polygon vertices with
      columns \code{x, y, poly_id, parcel_id, label, hemi, view, panel}.}
    \item{\code{boundaries}}{Tibble of boundary edges with columns
      \code{x, y, xend, yend, panel}.}
  }
}
\description{
Projects surface atlas geometry into 2D polygon panels for each
hemisphere and view. Results are memoised so repeated calls with
identical arguments return cached data.
}
