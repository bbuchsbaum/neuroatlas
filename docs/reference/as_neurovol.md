# Memoised version of .as_neurovol_unmemoised This is the function that should be called by get_template

Memoised version of .as_neurovol_unmemoised This is the function that
should be called by get_template

## Usage

``` r
as_neurovol(path_or_py_obj)
```
